# Beatsense
Beatsense is an AI-powered music exploration tool that uses Spotify audio features and unsupervised learning (K-Means clustering) to recommend tracks similar to a user’s selections. It includes a React frontend, Flask API backend, and full Docker support for easy deployment.
